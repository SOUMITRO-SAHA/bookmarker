import { TooltipProvider } from "@radix-ui/react-tooltip";
import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import {
  Example,
  Examples,
  Note,
  Title,
  VariantsTable,
  VariantColumn,
  RowTitles,
  CustomArgsTable,
  VariantRow,
} from "@/components/Default-Storybook";

import { PasswordField } from "./PasswordField";

<Meta title="UI/Input/PasswordField" component={PasswordField} />

<Title
  title="Passowrd Field"
  subtitle="Version 0.0.1 - Last Update: 25 Feb 2024"
/>

# Definition

The Password Field component allows users to toggle between displaying the password as plain text and hiding it for security.

# Structure

<Examples
  title="Password Field"
  footnote={
    <ul>
      <li>
        <strong>Hide Password:</strong> Indicates when the password is hidden.
      </li>
      <li>
        <strong>Show Password:</strong> Indicates when the password is visible.
      </li>
    </ul>
  }
>
  <Example>
    <PasswordField />
  </Example>
</Examples>
